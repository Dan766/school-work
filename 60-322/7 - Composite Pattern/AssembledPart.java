import java.util.ArrayList;


public class AssembledPart extends MyStore{

	public void includesComponent(String part, String comp) {
		int currentItemIndex = parts.size()-1;
		int compItemIndex = comp.charAt(1)-48; 
		this.parts.get(currentItemIndex).leafs.
		
	}
}
